# Managed by Ansible. Local HTTP terminates at Caddy over loopback.
{{ caddy_hostname }} {
    tls /etc/caddy/certificates/school-bell.crt /etc/caddy/certificates/school-bell.key

    request_body {
        max_size 4KB
    }

    reverse_proxy 127.0.0.1:{{ school_bell_webhook_port }}
}
